<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta property="og:title" content="Consultar" />
    <meta property="og:description" content="Deixamos aqui, com muito carinho, todas as informações importantes para o grande dia" />
    <meta property="og:image" content="https://www.tawanyerenato.com.br/images/image-5.jpg" />
    <meta property="og:url" content="https://www.tawanyerenato.com.br" />
    <meta property="og:type" content="website" />

    <title>Consultar</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= render "layouts/favicons" %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,200,0,0" />
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@400&display=swap" rel="stylesheet">
    <script src="https://sdk.mercadopago.com/js/v2"></script>
    <script src="https://www.mercadopago.com/v2/security.js" view="checkout" output="deviceId"></script>

    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-neutral font-canvas min-h-screen flex flex-col">
    <!---<p class="notice"><%#= notice %></p>
    <p class="alert"><%#= alert %></p>--->

    <%= render 'layouts/header' unless params[:controller].include?('users/sessions') %>
    <main class="flex-1"><%= yield %></main>
    <%= render 'layouts/footer' unless params[:controller].include?('users/sessions') %>

    <!-- Botão de Voltar ao Topo -->
    <div>
      <button onclick="scrollToTop()" class="btn bg-secondary fixed bottom-5 right-5 z-50 hover:bg-secondary text-white font-jost btn-top max-[768px]:relative max-[768px]:right-0 max-[768px]:top-1 max-[768px]:left-32 max-[768px]:mb-5">
        Voltar ao topo
      </button>
    </div>
  </body>

  <style>
    .font-jost {
      line-height: 2;
      letter-spacing: 1px;
      font-family: 'Jost', sans-serif;
    }
  </style>

  <script>
    function scrollToTop() {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    setTimeout(() => {
      if (deviceId && !sessionStorage.getItem('deviceId')) {
        sessionStorage.setItem('deviceId', deviceId);
      }
    }, 6000);
  </script>
</html>
